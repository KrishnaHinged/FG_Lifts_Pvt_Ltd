import colorsConfig from '@/design-system/tokens/colors'
import typographyConfig from '@/design-system/tokens/typography'

export const THEME = Object.freeze({
  COLORS: Object.freeze(colorsConfig),
  TYPOGRAPHY: Object.freeze(typographyConfig)
})

export default THEME
